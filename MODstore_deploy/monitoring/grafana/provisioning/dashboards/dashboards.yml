apiVersion: 1

providers:
  - name: MODstore
    orgId: 1
    folder: MODstore
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
